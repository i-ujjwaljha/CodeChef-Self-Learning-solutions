#include <bits/stdc++.h>
using namespace std;

int main() 
{
	int t;
	cin>>t;
	while(t--)
	{
		int n,a,b,c;
		cin>>n>>a>>b>>c;
		if((a+b+c)>=2*n)
		{
			if((a+c)>=n && b>=n)
			   cout<<"Yes"<<endl;
			   
			else
			   cout<<"No"<<endl;
		}
		
		else
		  cout<<"No"<<endl;
	}
	return 0;
}
